# LLM endpoint (e.g. local LLaMA server)
LLM_SERVER_URL=http://llm:8000/v1/chat/completions

# SearxNG endpoint
SEARXNG_URL=http://searxng:8081/search
SEARXNG_TIMEOUT=15

# Local memory path (inside container)
MEMORY_DB_PATH=/data/biological_memory
USER_PROFILE_FILE=/data/user_profile.txt
EPISODE_LOG_FILE=/data/episodes_log.jsonl
